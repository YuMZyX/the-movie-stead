import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#435585'
    },
    secondary: {
      main: '#F5E8C7'
    }
  }
})

export default theme